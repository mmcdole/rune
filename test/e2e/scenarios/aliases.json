{
  "description": "Aliases (45_aliases.lua): one wiring proof. Alias forms and edge cases live in lua/alias_test.go.",
  "scenarios": [
    {
      "name": "aliases expand before sending",
      "init_lua": ["rune.alias.exact('gg', 'cast gate')"],
      "steps": [
        { "connect": true },
        { "type": "gg" },
        { "expect_sent": "cast gate" }
      ]
    }
  ]
}
